services:
  frontend:
    build:
      context: ./frontend
    container_name: react
    ports:
      - "5173:5173"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    container_name: form-container
    env_file:
      - ./backend/.env
    ports:
      - "8000:5000"